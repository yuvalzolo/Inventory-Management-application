How to execute the app:
open 2 terminals - one for backend folder and one for frontend folder

baceknd folder:
npm install
npm start

frontend folder :
npm install
npm run ng serve

After the 2 servers are running open the browser on url http://localhost:4200 (but the api is listening on port 3000)

